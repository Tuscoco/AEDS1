#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double x = 3.14;
    double y = 400.5;
    double z,zz;

    //z = max(x,y); Retorna o maior valor entre 
    //zz = min(x,y); Retorna o menor valor

    //z = pow(x,2); Potencia
    //zz = pow(y,2);

    //z = sqrt(x); Raiz quadrada
    //zz = sqrt(y);

    //z = abs(-3); Módulo
    //zz = abs(-1000);

    //z = round(x); Arredonda para menos
    //zz = ceil(x); Arredonda para mais

    cout << z << '\n';
    cout << zz << '\n';

    return 0;
}